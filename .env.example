# ============================================================================
# Agri Cop — Environment Variables
# ============================================================================
#
# Copy this file to `.env` and fill in real values from your
# ProtoNest dashboard (https://api.protonestconnect.co).
#
# When running in DEMO MODE (toggle in the app Header) none of
# these values are needed — the app uses built-in mock data instead.
#
# ──────────────────────────────────────────────────────────────────────

# Your ProtoNest device ID (case-sensitive)
VITE_DEVICE_ID="your-device-id-here"

# Email address registered on ProtoNest
VITE_USER_EMAIL="your-email@example.com"

# Secret key from the ProtoNest dashboard
VITE_USER_SECRET="your-secret-key-here"

# WebSocket endpoint
VITE_WS_URL="wss://api.protonestconnect.co/ws"

# REST API base URL
VITE_API_BASE_URL="https://api.protonestconnect.co/api/v1/user"
